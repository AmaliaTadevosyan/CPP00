#ifndef BOOK

# define BOOK

#include <sstream>
#include <iostream>
#include <iomanip>

bool        isNumber(std::string str);
bool        myIsAlpha(std::string str);
std::string my_resize(std::string str);
#endif